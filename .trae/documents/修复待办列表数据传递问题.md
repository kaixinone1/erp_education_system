# 问题跟踪方案

## 目标
找出 `教师ID无效` 提示在哪一步产生

## 实施步骤

### 1. 在 dashboard/index.vue 添加调试
在 `handleTodo` 函数中添加 alert：
```javascript
const handleTodo = (row: any) => {
  console.log('处理待办:', row)
  const originalTodo = rawTodoList.value.find(todo => todo.id === row.id)
  
  // 添加调试
  alert(`handleTodo 调试:\noriginalTodo: ${JSON.stringify(originalTodo)}\n\nteacher_id: ${originalTodo?.teacher_id}`)
  
  if (originalTodo) {
    selectedTodo.value = {
      id: originalTodo.id,
      teacher_id: originalTodo.teacher_id,
      teacher_name: originalTodo.teacher_name,
      checklist_name: originalTodo.checklist_name,
      task_items: originalTodo.task_items,
      total_tasks: originalTodo.total_tasks,
      completed_tasks: originalTodo.completed_tasks,
      status: originalTodo.status,
      created_at: originalTodo.created_at
    }
    drawerVisible.value = true
  }
}
```

### 2. 在 ChecklistDrawer.vue 添加调试
在 `handleInternalForm` 函数开头添加 alert：
```javascript
const handleInternalForm = async (task: any) => {
  // 添加调试
  alert(`handleInternalForm 调试:\nprops.todoData: ${JSON.stringify(props.todoData)}\n\nteacher_id: ${props.todoData?.teacher_id}`)
  
  const tableName = task.目标
  const teacherName = props.todoData?.teacher_name
  let teacherId = props.todoData?.teacher_id
  
  // ...后续代码
}
```

### 3. 测试步骤
1. 刷新页面
2. 点击待办事项
3. 记录第一个弹窗内容（handleTodo）
4. 点击"填报"按钮
5. 记录第二个弹窗内容（handleInternalForm）
6. 对比两个弹窗的 teacher_id 值

### 4. 预期结果
- 如果第一个弹窗显示 `teacher_id: 273`，第二个弹窗显示 `teacher_id: undefined` → 问题在数据传递过程中
- 如果第一个弹窗显示 `teacher_id: undefined` → 问题在从 API 获取数据时
- 如果两个弹窗都显示 `teacher_id: 273` → 问题在检查逻辑中

## 修复方案
根据调试结果，确定具体问题后修复