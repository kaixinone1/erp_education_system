修复前端修改任职状态后，数据没有自动汇集到中间表的问题。

## 修复内容

### 修复1：修复 `id_card` 变量未定义错误（关键）
**文件**: `backend/routes/status_change_routes.py`
- 在获取 `teacher_row` 后，添加 `id_card = teacher_row[2]` 获取身份证号
- 在使用 `id_card` 的地方添加空值检查

### 修复2：改进错误处理和反馈
**文件**: `backend/routes/status_change_routes.py`
- 将数据汇集失败的信息返回给前端
- 让前端能够感知数据汇集是否成功

### 修复3：统一前端状态更新逻辑
**文件**: `frontend/src/views/data/GenericDataView.vue`
- 先调用API，成功后更新前端状态
- 确保前后端状态一致

### 修复4：验证字段名一致性
检查 `status_change_routes.py` 和 `retirement_data_routes.py` 中的字段名是否一致

## 验证方案
修复后验证：
1. 修改教师任职状态为"退休"
2. 检查后端控制台是否有错误
3. 检查 `retirement_report_data` 表是否有新记录
4. 检查 `todo_work_items` 表是否创建了待办清单