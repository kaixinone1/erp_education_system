修复从业务清单打开报表模板时无法获取数据的问题。

## 修复内容

### 修复1：修复变量名错误（关键）
**文件**: `frontend/src/views/report/ReportView.vue` 第131行
- 将 `const result = await response.json()` 改为 `const result = await dataResponse.json()`
- 这是导致数据无法加载的关键bug

### 修复2：添加调试日志
**文件**: `frontend/src/components/ChecklistDrawer.vue`
- 在 `handleFill` 函数中添加跳转参数日志，便于追踪问题

**文件**: `frontend/src/views/report/ReportView.vue`
- 添加 API 调用参数日志
- 添加数据获取结果日志

### 修复3：增强错误处理
**文件**: `frontend/src/views/report/ReportView.vue`
- API 调用失败时显示错误提示，而不是静默失败
- 添加更多的错误信息

## 影响评估
- 只修改前端代码，不影响后端
- 修复后报表数据提取功能将正常工作
- 调试日志有助于后续问题排查