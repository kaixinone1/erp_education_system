## 问题分析和解决方案

### 一、表名翻译现状
当前 `translate_table_name` 使用规则：
- 格式: `[模块前缀][核心实体]_[业务对象]`
- 示例: `教师人才类型` -> `teacher_talent_info`
- 字典表: `dict_` 前缀

### 二、字段名翻译现状
当前 `translate_field_name` 使用规则：
- 优先匹配全局映射表
- 使用简单词汇映射
- 示例: `姓名` -> `name`, `身份证号码` -> `id_card`

### 三、需要完善的内容

1. **表名翻译规则文档化**
   - 核心实体字典 (CORE_ENTITY_DICT)
   - 业务对象字典 (BUSINESS_OBJECT_DICT)
   - 模块前缀规则

2. **字段名翻译增强**
   - 扩展全局字段映射表
   - 添加更多常见字段翻译

3. **代码编写规范**
   - 后端英文（表名、字段名）
   - 前端中文（显示名称）
   - 写入规范文档

4. **菜单挂载规则**
   - 文件夹类型模块：不挂载数据管理器
   - 功能类型模块：挂载数据管理器

请确认后我开始实施具体的修改。