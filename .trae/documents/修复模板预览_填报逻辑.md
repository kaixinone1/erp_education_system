## 实施计划

### 任务1：清单模板移到模板管理中统一管理

**修改内容：**
1. **前端修改** `template-manager/index.vue`：
   - 添加"清单模板"标签页，与"文档模板"并列
   - 清单模板管理界面：列表、创建、编辑、删除
   - 清单模板表单：名称、触发条件、任务项列表、关联文档模板

2. **后端修改** `template_field_mapping_routes.py`：
   - 添加清单模板的CRUD API
   - 清单模板与文档模板关联

3. **数据库**：使用现有的 `business_checklist` 表

### 任务2：推送功能挂载到推送模板

**修改内容：**
1. **后端修改** `status_change_routes.py`：
   - 修改状态变更API，支持从清单模板获取配置
   - 清单模板中配置 `template_id` 字段
   - 创建待办时关联模板ID

2. **前端修改** `ChecklistDrawer.vue`：
   - 点击任务时，如果有 `template_id`，跳转到模板填报页面

### 任务3：完善中间表数据收集逻辑

**修改内容：**
1. **后端修改** `status_change_routes.py`：
   - 扩展数据收集逻辑，从更多源表获取数据
   - 完善 `retirement_report_data` 表的字段填充

2. **可能需要添加的表**：
   - 职务变动表
   - 工资/薪级表
   - 家庭成员表

---

**实施顺序：**
1. 任务1：清单模板移到模板管理
2. 任务2：推送功能挂载
3. 任务3：完善数据收集

**预计时间：** 每个任务约30-60分钟

请确认这个计划后，我将开始实施。