## 实施计划

### 指令1：创建业务清单主表 business_checklist

**数据库表结构：**
```sql
CREATE TABLE business_checklist (
    id SERIAL PRIMARY KEY,
    清单名称 VARCHAR(255) NOT NULL,
    触发条件 JSONB,
    任务项列表 JSONB,
    是否有效 BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**merged_schema_mappings.json 配置：**
- 表名：business_checklist
- 中文名：业务清单配置
- 字段映射

**初始数据：**
插入"退休教师呈报业务清单"，包含7项任务配置。

---

### 指令2：创建待办工作项表 todo_work_items

**数据库表结构：**
```sql
CREATE TABLE todo_work_items (
    id SERIAL PRIMARY KEY,
    教师ID INTEGER,
    清单ID INTEGER,
    清单名称 VARCHAR(255),
    任务项列表 JSONB, -- 包含每项任务的完成状态
    总任务数 INTEGER,
    已完成数 INTEGER,
    状态 VARCHAR(50), -- pending/completed
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

### 指令3：实现状态变更触发API

**修改 backend/routes/status_change.py：**
- 当状态变为"退休"时，查询 business_checklist
- 实例化清单到 todo_work_items
- 返回清单详情

**新增 API：**
- GET /api/todo-work/list - 获取待办列表
- POST /api/todo-work/update-status - 更新任务状态

---

### 指令4：开发主页待办工作区组件

**修改 Dashboard.vue：**
- 添加"待办工作"区域
- 调用 /api/todo-work/list 获取数据
- 显示格式："新增退休教师XXX业务清单（共7项，已完成Y项）"
- 点击展开抽屉组件

**创建 ChecklistDrawer.vue：**
- 显示7项任务列表
- 每项带复选框
- 根据类型实现不同交互

---

### 指令5：实现任务交互逻辑

**任务类型处理：**
1. **填报内部表**：路由到 GenericDataView.vue，自动填充
2. **跳转外部链接**：window.open 打开URL
3. **自动汇总**：调用后端API生成汇总文本
4. **签发退休证**：打开表单录入签收信息

---

### 指令6：创建清单管理模块

**在系统管理下创建：**
- 清单管理节点
- 使用通用数据管理界面
- 支持增删改查 business_checklist

---

## 实施顺序

1. 创建数据库表（business_checklist, todo_work_items）
2. 更新 merged_schema_mappings.json
3. 实现后端API
4. 开发前端组件
5. 创建管理模块
6. 测试验证

请确认此计划后，我将开始实施。