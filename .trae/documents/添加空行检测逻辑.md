## 修改内容

修改 `DataPreviewPanel.vue`，移除操作按钮的 `v-if="hasRowErrors(row)"` 限制，使所有行都显示操作按钮。

## 具体修改

将第130行：

```vue
<div v-if="hasRowErrors(row)" class="row-actions">
```

改为：

```vue
<div class="row-actions">
```

请确认后开始修改。
