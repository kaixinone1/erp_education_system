## 问题分析

1. **搜索功能未实现**：
   - 后端 `GET /{table_name}` 接口只支持 `filter` 参数，但前端传的是 `keyword`
   - 后端没有实现根据关键词搜索的逻辑

2. **编辑保存功能未实现**：
   - 后端 `PUT /{table_name}/{record_id}` 接口只是返回成功消息
   - 没有实际执行 SQL UPDATE 语句更新数据库

## 修复计划

### 1. 修复搜索功能
修改后端 `GET /{table_name}` 接口：
- 添加 `keyword` 参数支持
- 实现 SQL `LIKE` 搜索逻辑，在所有字段中搜索关键词
- 同时支持原有的 `filter` 参数

### 2. 修复编辑保存功能
修改后端 `PUT /{table_name}/{record_id}` 接口：
- 构建动态 SQL UPDATE 语句
- 根据传入的数据更新对应字段
- 使用主键 `id` 作为更新条件
- 返回实际更新结果

请确认后执行修改。