## 排查结果

### 1. ✅ 后端状态变更成功
- 日志显示匹配成功：`检查 退休 是否在 ['退休', '提前退休']: True`
- API 返回 200 OK

### 2. ✅ 待办工作已创建
数据库查询结果：
- 贺其清 (ID: 198) - 退休教师呈报业务清单 - 0/7 完成
- 王秀凤 (ID: 293) - 退休教师呈报业务清单 - 0/7 完成

### 3. ❓ 前端可能的问题
- Dashboard 调用 `/api/todo-work/list` 获取待办列表
- 使用 `pendingTodos` computed 过滤 `status === 'pending'`
- 但用户说首页没有看到

## 需要检查

1. **API 返回的数据格式**：检查 `/api/todo-work/list` 返回的数据中 `status` 字段的值
2. **前端过滤条件**：可能是 `status` 字段名不匹配（数据库是 `状态`，前端用 `status`）
3. **页面渲染问题**：检查表格是否正确渲染

## 解决方案

检查并修复字段名映射，确保前端能正确显示待办工作。