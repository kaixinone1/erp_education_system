## 问题诊断
用户截图显示的是旧版 RetirementForm.vue（导入HTML的页面），而不是我创建的填报页面 RetirementReportFill.vue。

## 修复步骤
1. 修改 router/index.ts，将 /retirement-form 路由指向新的 RetirementReportFill.vue
2. 删除旧的 RetirementForm.vue 组件引用
3. 确保 ChecklistDrawer.vue 中的"填报"按钮跳转到正确路由
4. 验证导航配置中的路由路径正确

## 预期结果
- 访问 /retirement-form 显示新的填报页面（带所有模板字段）
- 从推送清单点击"填报"正确跳转
- 页面显示完整的职工退休呈报表单