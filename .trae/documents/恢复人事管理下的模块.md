## 修改内容

修改 `cleanup_service.py`，数据清理时**不再清理导航配置**：

### 当前清理内容（应该保留的）：
1. ✅ 删除数据库表
2. ✅ 清理表名映射（table_name_mappings.json）
3. ✅ 清理字段配置（field_configs/*.json）
4. ✅ 清理字典表关联引用

### 应该**保留**的（不清理）：
- ❌ 导航菜单配置（navigation.json）- **取消清理**

这样清理数据后，导航菜单结构仍然保留，重新导入数据后菜单可以直接使用。