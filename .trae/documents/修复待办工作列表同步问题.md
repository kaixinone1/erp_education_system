修复业务清单完成后，待办工作列表中"共 X 项，已完成 Y 项"没有同步更新的问题。

## 修改内容

**文件**: `ChecklistDrawer.vue`

### 修改1: 添加 `status-changed` 事件到 emits 定义
在原有的 emits 定义中添加新的事件。

### 修改2: 在任务状态更新成功后触发事件
在 `handleTaskComplete` 函数中，当 API 调用成功后，触发 `status-changed` 事件通知父组件刷新列表。

## 预期效果
用户勾选任务完成后，待办工作列表会自动刷新，显示最新的"共 X 项，已完成 Y 项"数据。