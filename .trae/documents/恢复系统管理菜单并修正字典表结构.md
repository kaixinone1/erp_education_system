## 修改内容

### 1. 恢复系统管理菜单
修改 `clear_all_configs.py`，在重置 navigation.json 时保留系统管理菜单（元模块）：
- 系统管理
  - 模块管理
  - 字典管理（空，等待动态添加）

### 2. 修正字典表结构（可选）
如果只需要 id + name 两个字段，修改 `_create_dictionary_table` 方法：
```sql
CREATE TABLE {table_name} (
    id SERIAL PRIMARY KEY,
    name VARCHAR(50) NOT NULL
)
```

或者保留4个字段（id, name, created_at, updated_at）也可以。